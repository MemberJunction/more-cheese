---
"@mj-more-cheese-demo/entities": minor
"@mj-more-cheese-demo/server": minor
"@mj-more-cheese-demo/ng": minor
---

Demo data: close two empty buckets, six committees, a real staff workspace, and addresses that survive being looked at.

The platform seed migration is re-emitted because the staff workspace grew (8 saved views, 7 queries, 8 notifications). No schema change — the seed carries data only.
